# starsounds

A binaural beat designer that runs in the browser. Use it with headphones. Drag the five tones around the field to tune them, then mix in white, pink or brown noise.

It is a single self-contained page: open `index.html`, or serve the folder from any static host.
